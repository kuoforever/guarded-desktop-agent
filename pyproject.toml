[build-system]
requires = ["setuptools>=77.0.3"]
build-backend = "setuptools.build_meta"

[project]
name = "guarded-desktop-agent"
version = "0.1.0"
description = "Durable, safety-governed desktop agent and MCP server for Windows."
readme = "README.md"
requires-python = ">=3.11,<3.14"
license = "Apache-2.0"
license-files = ["LICENSE"]
keywords = ["desktop-agent", "mcp", "windows", "uiautomation"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Win32 (MS Windows)",
    "Operating System :: Microsoft :: Windows",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Desktop Environment",
]
dependencies = [
    "mcp>=1.27.2,<2",        # MCP server/client SDK; tested stdio contract
    "mss>=9.0",              # screen capture
    "uiautomation>=2.0.18",  # UIA accessibility tree + patterns
    "psutil>=5.9",           # process-tree for the foreground gate
    "pillow>=10.0",          # bbox annotation in smoke tests
    "winrt-runtime~=3.2.1; sys_platform == 'win32'",
    "winrt-Windows.Foundation~=3.2.1; sys_platform == 'win32'",
    "winrt-Windows.Foundation.Collections~=3.2.1; sys_platform == 'win32'",
    "winrt-Windows.Data.Xml.Dom~=3.2.1; sys_platform == 'win32'",
    "winrt-Windows.Globalization~=3.2.1; sys_platform == 'win32'",
    "winrt-Windows.Graphics.Imaging~=3.2.1; sys_platform == 'win32'",
    "winrt-Windows.Media.Ocr~=3.2.1; sys_platform == 'win32'",
    "winrt-Windows.Storage.Streams~=3.2.1; sys_platform == 'win32'",
    "winrt-Windows.UI.ViewManagement~=3.2.1; sys_platform == 'win32'",
    "winrt-Windows.UI.Notifications~=3.2.1; sys_platform == 'win32'",
    "pywin32>=312; sys_platform == 'win32'",
    "comtypes>=1.4.16; sys_platform == 'win32'",
]
# Mouse/keyboard input is done via ctypes (keybd_event/mouse_event) and
# uiautomation.SendKeys — no pyautogui dependency needed.

[project.urls]
Homepage = "https://github.com/kuoforever/guarded-desktop-agent"
Repository = "https://github.com/kuoforever/guarded-desktop-agent"
Issues = "https://github.com/kuoforever/guarded-desktop-agent/issues"
Changelog = "https://github.com/kuoforever/guarded-desktop-agent/blob/main/CHANGELOG.md"

[project.optional-dependencies]
dev = ["pytest>=8", "ruff>=0.5,<0.16", "mypy>=1.11", "build>=1.2", "setuptools>=77.0.3"]
agent-openai = ["openai>=1.68,<3"]
agent-anthropic = ["anthropic>=0.77,<1"]
agent = ["openai>=1.68,<3", "anthropic>=0.77,<1"]
# Observability is opt-in. Without it the telemetry port stays a no-op and
# opens no connection, so offline runs and CI never depend on a collector.
observability = ["opentelemetry-sdk>=1.25", "opentelemetry-exporter-otlp>=1.25"]
# Proof of concept only. The main package never imports temporalio, and no
# runtime path depends on a Temporal server being reachable.
temporal = ["temporalio>=1.7"]

[project.scripts]
guarded-desktop-mcp = "computer_use_mcp.server:main"
guarded-desktop-agent = "computer_use_agent.cli:main"
# Compatibility aliases retained for existing MCP clients and operator scripts.
computer-use-mcp = "computer_use_mcp.server:main"
computer-use-agent = "computer_use_agent.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
computer_use_agent = ["assets/*.docx"]

[tool.ruff]
line-length = 100

[tool.pytest.ini_options]
markers = [
    "openai_integration: opt-in live OpenAI Responses API test with a harmless fake MCP child",
    "anthropic_integration: opt-in live Claude Messages API test with a harmless fake MCP child",
]

[tool.mypy]
# Type checking is on by default for everything under src/, so a new module is
# checked the day it is written. The exemptions below are pre-existing debt.
#
# The rule for this list: it may only shrink. Adding a module to it is not a
# way to land new code, and a module removed from it must stay clean.
python_version = "3.11"
files = ["src"]
# Windows, UIA, and provider SDK stubs are incomplete or absent. Scoped
# per-module overrides are preferred over silencing a whole error class.
ignore_missing_imports = true
warn_unused_ignores = false

[[tool.mypy.overrides]]
module = "computer_use_agent.campaign"
ignore_errors = true

[[tool.mypy.overrides]]
module = "computer_use_agent.campaign_host_status"
ignore_errors = true

[[tool.mypy.overrides]]
module = "computer_use_agent.campaign_resume"
ignore_errors = true

[[tool.mypy.overrides]]
module = "computer_use_agent.cli"
ignore_errors = true

[[tool.mypy.overrides]]
module = "computer_use_agent.config"
ignore_errors = true

[[tool.mypy.overrides]]
module = "computer_use_agent.continuation"
ignore_errors = true

[[tool.mypy.overrides]]
module = "computer_use_agent.demo_campaign"
ignore_errors = true

[[tool.mypy.overrides]]
module = "computer_use_agent.desktop_mcp"
ignore_errors = true

[[tool.mypy.overrides]]
module = "computer_use_agent.evaluation"
ignore_errors = true

[[tool.mypy.overrides]]
module = "computer_use_agent.executor_final_reconciliation"
ignore_errors = true

[[tool.mypy.overrides]]
module = "computer_use_agent.plan_store"
ignore_errors = true

[[tool.mypy.overrides]]
module = "computer_use_agent.privacy.core"
ignore_errors = true

[[tool.mypy.overrides]]
module = "computer_use_agent.privacy.image"
ignore_errors = true

[[tool.mypy.overrides]]
module = "computer_use_agent.providers.anthropic"
ignore_errors = true

[[tool.mypy.overrides]]
module = "computer_use_agent.providers.anthropic_final"
ignore_errors = true

[[tool.mypy.overrides]]
module = "computer_use_agent.providers.anthropic_planner"
ignore_errors = true

[[tool.mypy.overrides]]
module = "computer_use_agent.providers.openai"
ignore_errors = true

[[tool.mypy.overrides]]
module = "computer_use_agent.providers.openai_final"
ignore_errors = true

[[tool.mypy.overrides]]
module = "computer_use_agent.providers.openai_planner"
ignore_errors = true

[[tool.mypy.overrides]]
module = "computer_use_agent.recovery"
ignore_errors = true

[[tool.mypy.overrides]]
module = "computer_use_agent.release"
ignore_errors = true

[[tool.mypy.overrides]]
module = "computer_use_agent.report"
ignore_errors = true

[[tool.mypy.overrides]]
module = "computer_use_agent.run_lock"
ignore_errors = true

[[tool.mypy.overrides]]
module = "computer_use_agent.runner"
ignore_errors = true

[[tool.mypy.overrides]]
module = "computer_use_agent.tool_registry"
ignore_errors = true

[[tool.mypy.overrides]]
module = "computer_use_agent.trace"
ignore_errors = true

[[tool.mypy.overrides]]
module = "computer_use_agent.types"
ignore_errors = true

[[tool.mypy.overrides]]
module = "computer_use_mcp.audit"
ignore_errors = true

[[tool.mypy.overrides]]
module = "computer_use_mcp.drivers.windows"
ignore_errors = true
